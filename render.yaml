services:
  - type: web
    name: Portifolio
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn demoproject.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.12"
